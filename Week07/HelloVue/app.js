let hello = new Vue({
    el: '#hello',
    // You can use CSS selector to identify the element to replace, or
    //  you can provide a direct reference to the element
    //el: document.getElementById('hello')

    // The data object
    data: {
        message: 'Good Afternoon',
        courseName: 'CS4220'
    }

})
